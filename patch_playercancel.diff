--- src/Player.js	2024-05-24 16:00:00.000000000 -0400
+++ src/Player.js	2024-05-24 16:00:00.000000000 -0400
@@ -280,9 +280,11 @@

     this.chopTarget = null;
     this.mineTarget = null;
+    this.craftTarget = null;
+    this.craftQuantity = 0;

-    if (this.state === PlayerState.CHOPPING || this.state === PlayerState.MINING) {
+    if (this.state === PlayerState.CHOPPING || this.state === PlayerState.MINING || this.state === PlayerState.CRAFTING) {
       this.state = PlayerState.IDLE;
     }
   }
